.venv/
__pycache__/
*.pyc
*.log
*.pid

# Local-only tooling (e.g. cloudflared binary for the tunnel) — not committed
tools/

# Committed for clone-and-run: data/corpus/ (source PDFs), data/uploads/ (working
# copies), data/chroma_db/ (vector index) and data/app.db (users + documents).
# Only generated Word exports stay out of git.
data/exports/

# Legacy (pre-restructure) artifacts
data/documents/
data/chroma/
data/doku.db
